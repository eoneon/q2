<h1>Create Value</h1>
<!-- for #new, we instantiate item_field instance variable in the controller, see #edit -->
<%= render 'field_values/form', item_field: @item_field, field_value: @field_value %>
<!-- likewise, from #new we already have the instantiated item_field which we can use to get its category -->
<%= link_to 'Return to parent field', [@item_field.category, @item_field] %>
